<h1>New Booking</h1>

  <%= @booking.flight.from_airport.code%> to <%=  @booking.flight.to_airport.code %> <%=@booking.flight.takeoff_day%>
  <%= @booking.passengers.each do |p|%>
    <%= p.first_name%>
  <%end%>
<%= link_to 'Back', bookings_path %>
